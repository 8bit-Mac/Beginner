listOfWords = ()
numberOfLetters = 0
n = int(input("Number of words: "))
print("Enter the words")
for i in range(0,n):
   m = input()
   listOfWords.append(ele)
   if len(m) >= numberOfLetters:
       numberOfLetters = len(m)
       word = m
print(listOfWords)
print("Longest word: ", word)
print("Length : ",Letters)
